import { FeatureProps, HandlerProps } from "@/typings/index.js";

export class Schematic {
    static registerFeature = (args: FeatureProps): FeatureProps => {
        return args;
    }

    static registerHandler = (args: HandlerProps) => {
        return args;
    }
}
